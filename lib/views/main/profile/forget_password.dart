import 'package:flutter/material.dart';

class ProfileForgetPassword extends StatelessWidget {
  const ProfileForgetPassword({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
