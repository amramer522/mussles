import 'package:flutter/material.dart';

class ProfileLanguagesView extends StatelessWidget {
  const ProfileLanguagesView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
