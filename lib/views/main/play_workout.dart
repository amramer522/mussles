import 'package:flutter/material.dart';

class PlayWorkoutView extends StatelessWidget {
  const PlayWorkoutView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
