import 'package:flutter/material.dart';

class SelectHeightDialog extends StatelessWidget {
  const SelectHeightDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SimpleDialog();
  }
}
