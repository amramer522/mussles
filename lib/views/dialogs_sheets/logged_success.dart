import 'package:flutter/material.dart';

class LoggedSuccessDialog extends StatelessWidget {
  const LoggedSuccessDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SimpleDialog();
  }
}
