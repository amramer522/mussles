import 'package:flutter/material.dart';

class GenderOrHeightView extends StatelessWidget {
  const GenderOrHeightView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
